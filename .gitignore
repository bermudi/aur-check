# mailing-list archive: input that motivated the tool, not the tool itself.
# also provenance-sensitive — don't push to any remote without explicit review.
*.mbox
*.mbox.gz

# Local state and transcripts from the maintainer-only issue relay.
.ralph-loop/
